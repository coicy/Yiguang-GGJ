# 仓库约束

## 文档索引

以下为本仓库当前最新版、权威文档入口：

- `README.md`：仓库简介与入口说明。
- `AGENT.md`：仓库级开发约束与关键文档索引。
- `docs/GDD.md`：植物形态平台解谜游戏的核心玩法 GDD。
- `docs/api/player.md`：玩家形态、移动与能力的公共行为约束。
- `scenes/levels/Level_main.tscn`：当前权威的可玩手工关卡入口；由启动壳实例化。

其他历史或非权威文档统一放入 `docs/backup`，并在其中建立索引；除非用户明确要求详细搜索，否则不主动查询这些文档。

## 基本要求

- 开始任何仓库工作前，优先阅读本文件；随后按任务需要阅读文档索引中的权威文档。
- 新建文档时，先将文档路径和用途登记在 `WIP.md`；确认文档需要长期维护后，再将其提升到本文件的权威索引，并清理 `WIP.md` 中已完成条目。
- 代码、配置和文档变更应保持最小范围，避免覆盖或回退用户已有改动。
- 新增功能或修复问题时，应补充与变更匹配的测试，并在交付前执行相关验证。
- 运行命令、修改文件和提交变更前，确认操作范围仅限本仓库及任务所需路径。

## 关卡开发范围（2026-09-06 用户要求）

- 后续只开发正式手工关卡：`scenes/app/main.tscn → scenes/levels/Level_main.tscn → scenes/levels/level_01.tscn`。
- 不再新增或扩展 JSON 白盒、沙盒、演示等测试关卡；既有测试关卡保留为历史参考，不作为新功能的交付入口。
- 门、HUD、出口、机关和美术等新功能必须直接接入正式手工关卡，并从默认主入口验证。
- 自动化验证脚本仍可维护，用于检查正式手工关卡和可复用组件；不要为验证功能另建一套可玩测试关卡。
- 2026-09-06 用户追加授权：在 `scenes/levels/level_02.tscn` 还原桌面 `Yiguang.json` 的出生房间，并以其房顶长条地板为界还原上方全部空间（世界 y < 320）；使用现有手工组件，风格与 `level_01` 一致，可独立 F6 运行。此项不替换当前主入口，不扩展其他下方房间。
- 2026-09-06 最新用户纠正：`level_02` 中 `Ground13 + Ground14` 的整段跨度对应 `level_01/Geometry/Terrain/Floor2`，以源跨度 512×16 对应参考 1344×40，映射比例为 (2.625,2.5)。`level_02` 作为上部区块嵌入 `Level_main/Level01`，共用 `level_01` 的角色、HUD、背景与镜头；组合入口中隐藏并禁用整条 Floor2，使用 Ground13、Ground14 两段实际地板，保留中间机关缺口。保留用户已删除旧出生房间的编辑，在 `Level_main` 预览整体。
- 2026-09-06 最新用户指定通关条件：玩家触碰 `Level_main/Level01/Level02/Geometry/UpperEntities/Buttons/Button_59bb7020` 即完成关卡，替代此前的 `Button_bbbb4660`。HUD 常驻提示“爬到最高处即可通关”。由 `HandbuiltLevel` 接入一次性结算、计时/重试结果和重玩；保留按钮原有的方块触发连接。

## 分支合并边界（2026-09-06）

- 合并 `main` 时，正式关卡及其本地编辑、角色输入、移动、形态和能力行为以 `feat/coicy` 为准。
- 可以接入不改变上述行为的音频、素材、纯展示组件和工具改进；不要连带替换温室战斗关卡、新战斗操控或主线专属 HUD。
- `docs/backup/main_20260906/INDEX.md` 是本次上游设计与验证记录的历史索引，不代表本分支的当前实现或验证结果。

- 玩家攻击已按用户要求关闭；`attack` 保留为空绑定动作，玩家场景不接入 CombatController、攻击 Hitbox 或战斗输入循环。左键形态能力继续可用。

## Godot 约束

- 本仓库使用 Godot 4+；新增或修改项目文件时遵循 Godot 4 的 API 与项目结构。
- 不提交 Godot 导入缓存、编辑器生成文件、导出凭据或临时文件；具体忽略项以 `.gitignore` 为准。
- 场景、脚本、资源和项目配置应使用清晰、稳定且与现有项目一致的命名。

## 交付检查

- 检查 `git diff` 与 `git status`，确认只包含本任务相关变更。
- 运行可用的 Godot 项目检查、测试或导出验证；若无法运行，说明原因及未验证范围。
- 交付说明应包含变更文件、验证结果和已知限制。
