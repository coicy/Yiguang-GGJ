# 白盒实体行为规则

以下规则对应当前 JSON 目标矩形驱动实现。2026-09-05 的机制修复补齐了 B1、B2、B4 的目标引用；关卡布局、实体 IID 与目标坐标保持原数据。历史按固定铰点描述的实现已由本表替代。世界坐标包含 level 的 worldX/worldY 偏移。

## 按钮与方块

| 按钮 | 目标 | 初始矩形 → 终点矩形（x,y,width,height） |
| --- | --- | --- |
| B1 (448,304) | C1 + Door | C1：(480,240,32,64) → (528,304,64,32)；Door：(624,432,32,32) → (624,384,32,32) |
| B2 (144,304) | C2 + C6 | C2：(592,176,16,144) → (608,320,144,16)；C6：(560,240,32,16) → (672,288,16,32) |
| B3 (672,144) | C5 | (176,288,32,16) → (176,304,32,16) |
| B4 (0,160)，保存于 Level_1 | C3 + C4 | C3：(160,-128,32,80) → (160,16,32,96)；C4：(48,-128,32,128) → (48,16,32,128) |

- Button 的 Entity_ref 可以引用 Door 或实际移动方块；MoveableCube 的 Entity_ref 引用唯一终点标记。Door 使用自身开门配置，不需要额外终点实体。空按钮引用、未接线的门或有终点但无控制按钮的方块会报告数据问题。
- 按钮采用单次锁存；按住、重复接触和再次触发不会叠加运动，R 重开恢复初始状态。
- 运动先短暂视觉抖动，再按平滑曲线移动。旋转使用初始和终点变换求得的圆弧；B2 的 C2、C6 使用共同运动时长，整个旋转过程中保持刚性相对位置。当前数据导出的共同铰点为 (600,328)。
- 终点 MoveableCube 不创建重复实体；紧贴终点底边的 Ring 也是编辑用终点标记，不创建额外可钩挂点。
- B4 控制平台上的实际 Ring 在运动完成前隐藏且不可钩挂，完成后开启。Ring 随平台移动，平台尺寸变化时仍贴合底边。C3 在本数据中从 80 像素变为 96 像素高，画面和碰撞同步更新。
- 实体可视与碰撞边界采用 JSON 实例尺寸；按钮交互范围额外向上延伸 6 像素。

## 其他实体与能力

- Ground：一般地块，允许扎根；HardFloor：实体地块，禁止扎根。
- Damage / DamageMachine：接触即死；DamageMachine 暂用左右各 32 像素、64 像素/秒行程。
- Door：玩家触碰关联按钮后单次启动，默认向上移动 48 像素并保持打开；当前地图由 B1 同时控制 C1 与 Door。实体和碰撞同步移动，原门位置不残留静态地块碰撞，R 重开重新生成关闭的门。
- 可复用门场景为 `features/level/whitebox_door.tscn`，继承现有移动方块的物理运动。Inspector 中 `opening_mode` 选择 SLIDE / ROTATE；`open_offset` 为门的局部位移；`hinge_offset` 为局部铰点，`opening_degrees` 正值顺时针、负值逆时针（最大 180 度）；`motion_speed` 控制速度。关卡父节点收到按钮的 `pressed` 信号后调用门的 `open()`；`opened` 在运动完成时发出。
- Door 与 ExitGoal 分别承担实体开门与通关检测。主 JSON 关卡已经接线：B1（画面标为“出口按钮”）开启 Door，等待 `opened` 后解锁门右侧的 ExitGoal；只按按钮、门尚在运动中、或提前站在锁定出口时均不通关。
- Frog：毒雾。速度倍率在约 1 秒内从 1.0 降至 0.55，离开后约 1 秒恢复；稳定度消耗、恢复及枯萎遵循 GDD。
- Ring：仅成熟期可连接，候选列表仅包含已经开放的锚点。
- Wind：按 JSON 边界持续向左吹，墙体和移动方块遮挡实际风力与风效。
- 营养液/毒液：按住 E 连续吸收。达到一次阶段阈值即结束本轮吸收，显示“松开 E 后继续吸收”；必须释放再按下才能继续。离开并重新进入液体不会绕过该限制。
- 音效为场景局部的事件监听，不驱动玩法。玩家与关卡声音各限制为 6 个声部；脚步仅在实际地面移动时播放，吸收音有间隔，锁存按钮重复触发不重播。

## 出口、胜利与 HUD

- `JsonWhiteboxLevel.exit_door_iid` 指定需要开启的 Door；`exit_offset` 默认为 `(64,0)`，从门的初始底边中心计算出口位置。当前出口底边中心为 `(704,464)`，触发区与源门同为 `32×32`，位于门后的现有地面上；本轮不修改源 JSON 布局。
- 玩家触碰已解锁出口即通关，无额外按键；若解锁时玩家已经站在出口内，也能通关。完成信号只触发一次。
- 通关后停止计时、玩家输入与整个关卡的玩法更新，Interface 继续接收输入；胜利面板显示本次用时和死亡重试次数并播放一次胜利音效。点击“再玩一次”或按 R 重新载入主场景，恢复出生位置、幼芽形态、按钮/门、出口、计时与重试次数。
- HUD 为只读显示，不拦截鼠标玩法输入；显示形态、成长、稳定度、计时、重试次数、出口目标与三形态操作说明。低稳定度变色，毒雾、检查点、能力反馈与阶段变化后的 E 释放要求有即时提示。
- 死亡计入重试次数并回到最近检查点，保留现有机关状态；R 则重开整个关卡。素材来源记录于 [credits](credits.md)。

## 验证入口

- tests/scene/test_json_exit_hud.gd：主场景 HUD/资源/三形态提示、真实锁定出口与 B1 接触、运动完成后解锁、在出口内解锁自动通关、单次结算、玩法冻结，以及真实鼠标点击和 R 重开复位。
- tests/scene/test_button_door.gd：实际 Player 接触、非玩家过滤、平移与双向旋转、碰撞跟随与通道清空、单次锁存、主地图 B1/C1 联动和重新实例化复位。
- tests/scene/test_json_whitebox_level.gd：四个按钮引用与运动终点、B1 开门、B2 中途刚性、B4 Ring 前后门控、变尺寸挂点。
- tests/scene/test_absorption_session.gd：长按 E 不连跳阶段、释放后可继续、离开资源后不绕过限制。
- tests/unit/test_resource_controller.gd：毒区速度渐变与资源数据。
- tests/scene/test_player_audio.gd：真实移动、跳跃落地、形态与能力事件，站立静音和吸收节流；图形模式用于实际音频播放验收。
