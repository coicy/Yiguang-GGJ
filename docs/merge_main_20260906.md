# main 合并记录：保留关卡与操控，关闭攻击

日期：2026-09-06。当前分支基线 `5424e04`，上游合并来源 `3f1f107`。

## 用户要求与保留范围

- 正式关卡以本分支为准，包含合并前工作区中 `scenes/levels/level_01.tscn` 的未提交编辑。关卡源文件及两个本地 UID 已与备份逐字节核对。
- 保留 `main.tscn → Level_main.tscn → level_01.tscn` 入口及本分支相机设置；不切换温室或战斗测试关卡。
- 保留玩家场景、组合脚本、移动控制器、状态机、形态参数、能力控制器、资源控制器及关卡生命周期脚本，与基线文件内容一致。
- 保留原 A/D 移动、Space 跳跃/滑翔、左键扎根/藤蔓、E 吸收/攀爬；攻击按用户后续要求关闭。`attack` 动作保留但无绑定，不接入攻击循环、CombatController 或攻击 Hitbox。

## 采用的上游改进

- `SoundEmitter`、`AudioPalette`、默认混音总线、分层音效和素材来源：固定声音池、优先级、同帧去重、冷却和不连续重复的声音变体。
- `PlayerAudio` 选择性兼容：落地力度、速度/地表脚步、拔根/收腿/断藤、滑翔结束、毒液吸收反馈。沿用本地能力接口，不依赖主线战斗或新藤蔓信号。
- `TerrainSkin` / `TerrainSurface` 及地形美术资源；已有地形组件新增默认保持原行为的显示和平铺选项，不改变碰撞。
- 藤蔓锚点纯绘制改进、移动平台状态查询、按钮的可选按下美术变体。
- 上游新增素材和来源文件、离线音频制作工具、植物 UI Theme、Windows 内部评审导出配置。素材与组件可复用；本次未重新布局关卡或替换 HUD，未安装音频制作依赖或生成导出包。

主线新增的战斗、敌人、温室运行时、专属 HUD、关卡构建和捕获脚本及专属测试依赖另一套玩家 API，且会改变用户要求保留的玩法，本次不接入。对应上游设计和验收历史集中存放于 [历史索引](backup/main_20260906/INDEX.md)，不得当作本分支的当前验证结果。

## 验证

引擎：仓库已有 Godot 4.7.2。隔离工作区执行两轮导入，使 GDExtension 注册后完成 Spine 资源导入；验证进程使用工作区内临时 APPDATA，不改用户编辑器设置。

| 检查 | 结果 |
|---|---|
| 默认项目启动 | headless 120 帧，退出码 0，无脚本/资源错误 |
| Player、TerrainSkin、SoundEmitter 单场景启动 | F6 等价 headless 启动均退出码 0 |
| `test_level_01`、`test_level_main` | 通过 |
| `test_phantom_camera_integration` | 通过：死区、前视、CameraBounds |
| `test_handbuilt_floor_pieces` | 通过：自定义地形碰撞与展示组件隔离 |
| 单元/场景 `test_player_abilities`、`test_vine_direction` | 通过 |
| `test_player_audio` | 完整真实 Spine 与物理/能力实例，PASS |
| `test_sound_emitter` | 561 项检查，0 失败 |
| `test_attack_disabled` | 35 项检查，0 失败；三形态注入 attack/J 不影响待机、移动、位置/速度/动画，左键仍为形态能力 |
| 运行时 `res://` 引用 | 无缺失引用 |
| 本地关卡与核心操控源文件 | 与备份/基线内容一致，只有攻击绑定和纯声音反馈按要求调整 |

已知基线问题：`test_player_movement_response.gd` 在原工作区和合并工作区均失败，13 条失败完全一致，六组启动/刹车/转向测量值逐项一致。该旧测试依赖关卡内固定坐标的地形，失败根因尚未进一步定位；本次没有为满足旧测试而改动用户关卡或控制器，不宣称全部测试通过。失败涉及刹车距离/时间、静止位置及跳跃/空中转向检查，保留为独立待排查项。

环境提示：Godot 无法读取 Windows 根证书存储，合并前后均出现；上述测试不使用网络。最初干净工作区未完成第二轮 Spine 导入时的空骨骼错误已通过完整导入消除。上游原始许可文本与下载来源 HTML 保持原样，因此全资产 `git diff --check` 会报告其中既有的空白字符问题。

## 恢复与本地文件

合并前的场景、UID、主题和日志原始文件及补丁备份位于本机被忽略的 `build/merge-backup-20260906/`；验证日志位于合并工作区的 `build/merge-validation/`。日志与缓存不进入提交。原工作区另有未跟踪的 `scenes/level_01.tscn`，保持原样，不作为正式入口或纳入本次合并。
